<!DOCTYPE html>
<html lang="en" ng-app="cvApp">
<head>
    <meta charset="utf-8">
    <title><%= title %></title>
    
    <!-- JS -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.0/jquery.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.5.3/angular.min.js"></script>


    <!-- import jsPDF to work with pdf-file -->
    <script src="http://mrrio.github.io/jsPDF/dist/jspdf.debug.js"></script>
    <!-- CSS -->
    <link rel="stylesheet" href="stylesheets/style.css" />
    <link rel="stylesheet" href="stylesheets/normalize.css" />
    <link rel="stylesheet" href="fonts/font-awesome-4.2.0/css/font-awesome.min.css" /> <!-- needed for ruri class (form controls) -->
    
    
    <!-- ANGULAR FILES -->
    <script src="javascripts/controllers/controllers.js"></script>
    <script src="javascripts/directives/directives.js"></script>
    <script src="javascripts/main.js"></script>
    
    
    <script> // Google analytics 
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){(i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)})(window,document,'script','https://www.google-analytics.com/analytics.js','ga');ga('create', 'UA-76287851-1', 'auto');ga('send', 'pageview');




////////////////////// this function is part of __input functionality

    </script>
</head>
<body ng-controller="MenuCtrl as menu"> <!-- ng-controller="PersonCtrl as person" -->

        <% include templates/header.ejs %>

        <div class="main">                  <!--////////////////////////////////// BEGIN MAIN //////////////////////////////  -->
            <div class="left">
    			<iframe allowtransparency="true" class="preview-pane" type="application/pdf" width="100%" height="100%" 
    			frameborder="0" style="position:relative;z-index:1">Unfortunately, your browser does not support PDF preview:(
                <!-- You can download it though! <a href="javascript:savePDF('CV_'+person.fullName)" class="my-button icon-download">Get PDF</a> -->
                </iframe>
    		</div>
            <script>
                var person = <%- JSON.stringify(person) %>;
                preparePDF(person);     // TO DO: MOVE THIS TO MAIN.JS !!!!!!!!!!!
            </script>
        
            <div class="right center">   <!--////////////////////////////////// BEGIN RIGHT DIV //////////////////////////////  -->
                <div class="work-space">
                    <% include templates/work-space.ejs %>
                </div>
            </div>
        </div>
    
        <div class="footer">            <!--////////////////////////////////// FOOTER /////////////////////////////////////////  -->
            <% include templates/footer.ejs %>
        </div>
     <script src="javascripts/classie.js"></script>
        <script type="text/javascript">

 $(document).ready(function() {
    if (!String.prototype.trim) {
        (function() {
            var rtrim = /^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g;
            String.prototype.trim = function() {
                return this.replace(rtrim, '');
            };
        })();
    }
    [].slice.call( document.querySelectorAll( 'input.input__field' ) ).forEach( function( inputEl ) {
        // in case the input is already filled
        if( inputEl.value.trim() !== '' ) {
            classie.add( inputEl.parentNode, 'input--filled' );
        }
        // events:
        inputEl.addEventListener( 'focus', onInputFocus );
        inputEl.addEventListener( 'blur', onInputBlur );
    } );
    function onInputFocus( ev ) {
        classie.add( ev.target.parentNode, 'input--filled' );
    }
    function onInputBlur( ev ) {
        if( ev.target.value.trim() === '' ) {
            classie.remove( ev.target.parentNode, 'input--filled' );
        }
    }



        </script>
</body>
</html>
